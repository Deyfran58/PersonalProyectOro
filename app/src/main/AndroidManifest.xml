<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoldNet"
        tools:targetApi="31">

        <!-- Pantalla principal (Login) -->
        <activity
            android:name=".view.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla de registro -->
        <activity android:name=".view.RegisterActivity" />

        <!-- Pantalla de inicio o menú principal -->
        <activity android:name=".view.HomeActivity" />

        <!-- Pantalla de detalle de producto -->
        <activity android:name=".view.ProductDetailActivity" />

        <!-- Pantalla de historial de compras -->
        <activity android:name=".view.PurchaseHistoryActivity" />

        <!-- (Opcional) Pantalla para perfil de usuario -->
        <activity android:name=".view.ProfileActivity" />

    </application>

</manifest>
